version: "3.7"

services:
  php:
    build: .
    volumes:
      - ./:/app
    ports:
      - 80:80
    environment: 
      APACHE_DOCUMENT_ROOT: /app/public
  # db:
  #   image: mysql
  #   command: --default-authentication-plugin=mysql_native_password
  #   environment: 
  #     MYSQL_USER: vertigo
  #     MYSQL_PASSWORD: qwerty
  #     MYSQL_DATABASE: vertigo
  #     MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
  # adminer:
  #   image: adminer
  #   restart: always
  #   ports:
  #     - 8080:8080